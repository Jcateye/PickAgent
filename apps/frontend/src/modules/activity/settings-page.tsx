'use client'

import React from 'react'

export function SettingsPage() {
  return (
    <div style={{ padding: '48px', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '100%', color: 'var(--muted)' }}>
      <h1 style={{ fontSize: '24px', fontWeight: 600, color: 'var(--fg)', marginBottom: '16px' }}>系统设置 Settings</h1>
      <p>当前页面为原型占位符。开发完成后将提供组织、账号权限等设置。</p>
    </div>
  )
}
