{
  "name": "@pickagent/extension",
  "displayName": "SKU Ready Agent",
  "version": "0.1.0",
  "private": true,
  "description": "SKU Ready Agent browser extension frontend",
  "scripts": {
    "dev": "plasmo dev",
    "build": "plasmo build",
    "test": "rm -rf .test-build && tsc --rootDir src --outDir .test-build --module Node16 --moduleResolution Node16 --target ES2020 src/lib/ingest/fixture-smoke.test.ts && node .test-build/lib/ingest/fixture-smoke.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^1.16.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "plasmo": "^0.90.4",
    "typescript": "^5.8.3"
  }
}
